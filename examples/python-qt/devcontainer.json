{
  "name": "Python (Qt/GUI)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteEnv": {
    "UV_LINK_MODE": "copy",
    "UV_COMPILE_BYTECODE": "1"
  },
  "postCreateCommand": "pip install uv && (uv sync --locked 2>/dev/null || uv pip install --system -r requirements.txt 2>/dev/null || true)"
}
